# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# ── Виртуальные окружения ────────────────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── Артефакты проекта (генерируются кодом, не коммитим) ────────────────────
# tokenizer_corpus/ — сырой + объединённый (_combined.txt) корпус для токенизатора, большой
tokenizer_corpus/
# checkpoints/ — чекпойнты train.py (step_*.pt)
checkpoints/
# checkpoints_smoke/ — чекпойнты локальных смоук-тестов
checkpoints_smoke/
# data_cache/ — кэш dataset.py
data_cache/
# wandb/ — локальные логи Weights & Biases
wandb/
*.pt
*.pth

# tokenizer/ НЕ игнорируем — модель маленькая (~2MB) и нужна для воспроизводимости.
# Если хочешь всегда пересобирать её из tokenizer_train.py — раскомментируй:
# tokenizer/

# ── Логи ─────────────────────────────────────────────────────────────────
*.log

# ── Jupyter ──────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── OS / IDE ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Claude Code локальные настройки сессии ──────────────────────────────
.claude/
